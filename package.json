{
  "name": "vue-resume",
  "version": "1.0.0",
  "description": "1. 安装 Node.js 和 NPM 2. `npm i -g webpack` 3. `npm i babel-loader babel-core babel-preset-es2015 babel-preset-react` 4. `webpack` 5. 打开 page.html",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vue": "^2.4.3"
  }
}
